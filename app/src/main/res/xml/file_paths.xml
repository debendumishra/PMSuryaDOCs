<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Allow sharing files from internal cache (where we generate PDFs/ZIPs before copying to output folder) -->
    <cache-path name="shared_cache" path="." />
    <!-- Allow sharing files from external files directory -->
    <external-files-path name="shared_external_files" path="." />
    <!-- Allow sharing files from external cache directory -->
    <external-cache-path name="shared_external_cache" path="." />
</paths>
